spring:
  application:
    name: fxz-server-system
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: ${spring.application.name}
        file-extension: yaml
nacos:
  url: 124.222.103.118