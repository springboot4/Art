spring:
  application:
    name: fxz-auth
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: ${spring.application.name}
        file-extension: yaml


nacos:
  url: 127.0.0.1