spring:
  application:
    name: fxz-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8001
      config:
        server-addr: 127.0.0.1:8001
        group: DEFAULT_GROUP
        prefix: ${spring.application.name}
        file-extension: yaml
nacos:
  url: 47.94.42.44